package com.areeb.noteapp.utils

interface Constants {
    companion object {
        const val NOTE_ID = "noteID"
        const val _NOTE_ID = "noteID"
        const val SHARED_PREFERENCES = "shared_preferences"
        const val DARK_MODE = "dark_mode"
        const val NOTE_DB = "noteDB"
    }
}
